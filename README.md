# Demo project to illustrate an issue I'm having with Bun/Lingui

To demonstrate the issue:

- [Install Bun](https://bun.com/docs/installation)
- Clone repo
- Run `bun install` to install deps
- Run `bun run dev` to start the server
- Point your browser at `localhost:3000`
